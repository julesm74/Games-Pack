package com.wrcreedrs.games.listeners;

import org.bukkit.event.Listener;

import com.wrcreedrs.games.GamesLoader;

public class GamespackBlockListener implements Listener {
	GamesLoader plugin;
	public GamespackBlockListener(GamesLoader plugin) {
		this.plugin = plugin;
	}
}
